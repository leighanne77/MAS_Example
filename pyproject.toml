[project]
name = "provenance-by-construction"
version = "0.1.0"
description = "A reference implementation: make an unsourced number impossible to produce."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = ["pydantic>=2.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
